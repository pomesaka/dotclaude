## Gemini Search

`gemini` is a Google Gemini CLI. You can use it for web search.

Run web search via Task Tool with `gemini -p 'WebSearchの結果をまとめて: ...'`.

```bash
gemini -p "WebSearchの結果をまとめて: ..."
```
